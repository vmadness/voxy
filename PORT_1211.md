# Voxy port: dev (MC 26.2) → MC 1.21.1 + Sodium 0.8

Branch: `mc_1211`. Reference: `ref_m3t4f1v3/voxy` branch `mc_1211` (fetched as git remote `ref_m3t4f1v3`) — older voxy code ported to 1.21.1 with Sodium 0.6.13; useful for 1.21.1-era shims (fog, mixin plugin, compat), NOT for sodium API (we target Sodium `mc1.21.1-0.8.12-fabric`, whose API is closer to the 0.9.x dev already uses).

## Baseline facts

- Dev targets: MC 26.2, Sodium 0.9.1, Java 25, fabric-api 0.152.2, loom 1.16, new blaze3d GPU abstraction (`GpuDevice`, `RenderPass`, `GpuBuffer`, `pipeline.RenderPipeline`), `net.minecraft.resources.Identifier`, new fog (`client.renderer.fog.FogRenderer`/`FogData`), `LevelExtractor`, `DebugScreenEntryList`.
- Target: MC 1.21.1 (mojmap: `ResourceLocation`, pre-GpuDevice blaze3d, old fog, no LevelExtractor), Java 21, Sodium `mc1.21.1-0.8.12-fabric` (confirmed on Modrinth, released 2026-07-06), fabric-api `0.116.6+1.21.1`, loom `1.11-SNAPSHOT`+, loader `>=0.17.2`.
- Version-dependent surface: ~55 files import `net.minecraft.*`, 22 files use Sodium API, 12 files touch `com.mojang.blaze3d`, 13 MC mixins + 9 Sodium mixins + 10 Iris mixins, access widener, fabric.mod.json.
- Voxy has its own raw-GL layer (`client/core/gl/`, LWJGL GL45 DSA) — version-independent, keep as-is.
- Reference branch divergence: merge-base `41dd201d`, dev is 99 commits ahead, ref is 33 ahead; 145 files differ in src.

## Task list

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | Port build files (gradle.properties, build.gradle, fabric.mod.json, settings.gradle) to 1.21.1 / Sodium 0.8.12 / Java 21 | DONE | fabric.mod.json needed no code change (predicate generation already version-agnostic); nvidium/vivecraft/flashback/chunky disabled. Final toolchain: **loom 1.13.6 under the legacy `fabric-loom` plugin id** (1.14+ lines force disableObfuscation; old *releases* are still published, only old snapshots are pruned), Gradle 9.6 (no wrapper downgrade needed), `mappings loom.officialMojangMappings()`, all mods via `modImplementation`/`modCompileOnly`/`modRuntimeOnly`, full fabric-api bundle restored, access widener header switched `official`→`named`. One targeted workaround kept: `patchLoomVersionJar()` rewrites sodium 0.8.12's `Fabric-Loom-Version: 1.16.1` manifest attribute so loom 1.13.6's static-mixin-remap version gate accepts it. |
| 2 | First compile attempt; collect and cluster javac errors | DONE | With the proper mojmap remap pipeline: **430 errors**, all genuine 26.2→1.21.1 API differences (see clusters in progress log). Earlier 1045-error run was against an unmapped classpath and is superseded. |
| 3 | Mechanical renames (Identifier→ResourceLocation etc.) | DONE | Two waves (sonnet: client tree; gpt-5.6-terra: common tree), reviewed. Errors 430→370 (rest is design-pending clusters). Also: debug screen ported to MixinDebugScreenOverlay, compat mixins excluded via build.gradle sourceSets, VoxyClientInstance Java-21 constructor fix. |
| 4 | blaze3d backport: VoxyRenderSystem, RenderProperties, GL-backend access (GlCommandEncoder/GlRenderPass etc. don't exist in 1.21.1) | DONE | Implemented by gpt-5.6-sol per design doc: VoxyRenderSystem rewritten as raw-GL state snapshot/restore (+GlStateManager/BufferUploader cache invalidation), RenderProperties/LightMapHelper/NormalRenderPipeline ported, stale blaze3d AW entries removed, LightTexture.lightTexture AW added (javap-verified). Reviewed. |
| 5 | Fog backport: 26.x FogRenderer/FogData + Sodium FogParameters → 1.21.1 fog | DONE | Single fog holder VoxyFogSnapshot; capture via rewritten MixinFogRenderer-replacement populating it each terrain frame; sodium/iris consumers wired. Reviewed. |
| 6 | Sodium 0.9.1 → 0.8.12 API port (22 files + 9 sodium mixins + mixin accessors) | DONE | Implemented per design: 0.9-only collector mixins deleted, MixinSectionCollector + SodiumVisibilityBridge/SodiumGlInterop added, accessors retargeted to 0.8.12, ModelFactory/AbstractSectionRenderer fixed. Reviewed. |
| 7 | MC mixins retarget | DONE | DebugScreenOverlay mixin (T3 wave); LevelExtractor deleted, extraction rewired via LevelRenderer (blaze3d wave); MixinRenderSystem→initRenderer, MixinGlDebug→platform.GlDebug; ClientLevel/ClientChunkCache fixed. |
| 8 | Access widener rewrite for 1.21.1 mappings | DONE | named-namespace, every entry javap-verified against mapped 1.21.1 jar (bakery + blaze3d waves). |
| 9 | Model bakery / BlockEntity render backport (SoftwareModelTextureBakery, ReuseVertexConsumer, LightMapHelper) | DONE | Bakery + ReuseVertexConsumer ported (javap-verified; known gap: anyDarkendTex heuristic disabled — no 1.21.1 equivalent, same as ref port). LightMapHelper done in blaze3d wave. |
| 10 | Shader (GLSL) compatibility check | DONE | 43 GLSL files, no sodium coupling (self-contained voxy quad format). Shaders use #version 460: fine on real GPUs (GL 4.6); on Mesa/llvmpipe (WSL2 test env) needs MESA_GL_VERSION_OVERRIDE=4.6 MESA_GLSL_VERSION_OVERRIDE=460 — environment-only, no source change. |
| 11 | Full `./gradlew build` green | DONE | remapJar + full build green on Java 21; voxy-0.2.18-beta.voxyserver.1.jar produced. |
| 12 | Runtime smoke test (runClient) + review pass | DONE | Full in-world verification: server-pregen world + quickPlay join; voxy instance/world engine/VoxyRenderSystem (NormalRenderPipeline + MDICSectionRenderer) init clean; section ingest runs stably across worker threads ~5 min, zero voxy exceptions. Watch item: benign "section raced to save queue" WARNs (handled race). |
| 13 | Iris/Nvidium/Flashback/Chunky compat mixins | TODO | defer: disable when mod absent; proper iris port after core works |

## Decisions

- Orchestration: Fable 5 orchestrates/reviews only; code written by gpt-5.6-terra / Sonnet 5 (mechanical/normal) and gpt-5.6-sol (complex), via Codex plugin and Agent tool.
- Keep dev's `findSodiumVersion()` build logic, point it at `mc1.21.1-0.8.12-fabric` via modrinth maven.
- Iris/Nvidium/Vivecraft/Flashback/Chunky: defer, gate mixins on mod presence (ref branch has a `ClientVoxyMixinPlugin` doing exactly this — reuse the approach).
- Sodium fog: dev consumes sodium `FogParameters` in 6 files — verify what sodium 0.8.12 exposes on 1.21.1 before deciding shim vs direct use.

## Progress log

- 2026-07-18: Branch `mc_1211` created from dev; reference remote fetched; scoping complete; this doc created. All tasks TODO.
- 2026-07-18: Build files ported to 1.21.1 (task 1). Versions verified live on Modrinth: sodium `mc1.21.1-0.8.12-fabric`, lithium `mc1.21.1-0.15.0-fabric`, iris `1.8.8+1.21.1-fabric`, modmenu `11.0.3`, fabric-api `0.116.6+1.21.1`.
- 2026-07-18: Toolchain finding, first attempt (CORRECTED, see next entry): loom 1.14+ (all lines under the `net.fabricmc.fabric-loom` plugin id) unconditionally sets `disableObfuscation=true` for single-project applies — `mappings loom.officialMojangMappings()` fails with "Cannot use Mojang mappings in a non-obfuscated environment" and the `minecraft` dependency resolves as a raw obfuscated jar with no `net.minecraft.*` names (verified in a clean throwaway project). A first compile against that unmapped classpath produced 1045 bogus-mostly errors. The initial conclusion that the remap pipeline was unrecoverable was **wrong**: only old *snapshots* were pruned from maven.fabricmc.net; old *releases* (1.7.x–1.13.6) are fully available under the legacy `fabric-loom` plugin id (the `net.fabricmc.fabric-loom` marker just starts at 1.14).
- 2026-07-18: Toolchain redone properly (tasks 1+2 final):
  - **Final toolchain**: Gradle 9.6.0 (unchanged, works fine), **loom 1.13.6** via plugin id `fabric-loom` (newest pre-1.14 release; keeps the full obfuscation/mojmap remap pipeline), `mappings loom.officialMojangMappings()`, Java 21, MC 1.21.1. All mod deps moved to loom remap configurations (`modImplementation`/`modCompileOnly`/`modRuntimeOnly`, `smartDependency()` adapted; `runtimeOnlyMsk`→`modRuntimeOnlyMsk`), full `fabric-api` bundle restored via `modImplementation`, mirroring the reference branch's build.gradle structure. The earlier `patchAccessWidenerJar()` hack was removed entirely — with the real remap pipeline, intermediary AWs in dependency mods are handled by loom as normal.
  - `voxy.accesswidener` header changed `accessWidener v1 official` → `... named` (dev's 26.x tooling used `official`; mojmap-named is what loom 1.13 expects, matches the reference branch).
  - Remaining workaround: sodium `mc1.21.1-0.8.12` was built with loom 1.16.1 and `Fabric-Loom-Mixin-Remap-Type: static`; loom's `ArtifactMetadata.validateLoomVersion` refuses static-remap mods built by a newer loom ("Mod was built with a newer version of Loom (1.16.1), you are using Loom (1.13.6)"). `patchLoomVersionJar()` in build.gradle rewrites that one manifest attribute in a copy of the sodium jar. Loom 1.13.6 supports STATIC mixin remap; the gate is just a conservative major.minor compare. Lithium (loom 1.8.9/static), iris and modmenu (mixin-type) pass untouched.
  - `-Xmaxerrs 100000` added to javac args (temporary, for full error visibility during the port).
  - **Second compile (properly mapped classpath): 430 errors**, all real 26.2→1.21.1 differences (sanity: errors name `Identifier`/`ResourceLocation`, `FogParameters`, `ChunkSectionLayer` etc., no obfuscated noise). Full log: `/tmp/claude-1000/-home-mad-Projects-voxy/a529a125-96cd-4a57-8ed5-80034accf158/scratchpad/compile2.log`.
  - Error clusters (count — cause — example files):
    - 78 — long tail of misc 26.x API renames (model/render-state accessors like `getBlockStateModelSet`, `GpuFormat`/`MipmapStrategy`, `ARGB`, `PalettedContainerFactory`, packed-UV helpers, Gson `Strictness`, ...) — `WorldImporter.java`, `ModelFactory.java`, `SoftwareModelTextureBakery.java`
    - 63 — `Identifier` → `ResourceLocation` (task 3) — `DebugEntries.java`, `SodiumConfigBuilder.java`, `WorldIdentifier.java`
    - 42 — misc MC renames/moves (SectionPos `x()`/`z()`, `getMinSectionY`, `mainRenderTarget`, `BlockAndTintGetter`/`BakedQuad` moves) — `VoxyRenderSystem.java`, `ModelFactory.java`, `ReuseVertexConsumer.java`
    - 38 — sodium 0.9.x API absent in 0.8.12 (`ChunkSectionLayer`, `SectionStorage`, `VisibleChunkCollector`, `CardinalLighting`) (task 6) — `ModelFactory.java`, `MixinDefaultChunkRenderer.java`, `MixinRenderSectionManager.java`
    - 37 — `@Override` "does not override" (downstream of missing supertypes, will collapse) — `DebugEntries.java`, `ModelFactory.java`
    - 35 — blaze3d GpuDevice-era abstraction missing in 1.21.1 (`com.mojang.blaze3d.opengl/buffers/textures`, `GpuBuffer(Slice)`, `GpuSampler`, `GlTextureView`, `DepthStencilState`) (task 4) — `VoxyRenderSystem.java`, `RenderProperties.java`, `LightMapHelper.java`
    - 32 — compat mixins for disabled/absent mods (flashback/chunky/vivecraft/nvidium) (task 13, expected) — `MixinFlashbackRecorder.java`, `MixinFabricWorld.java`, `MixinRenderPipeline.java`
    - 26 — 26.x debug screen API (`DebugScreenEntry`/`Entries`/`Displayer`, `client.gui.components.debug`) (task 7) — `DebugEntries.java`, `VoxyDebugScreenEntry.java`, `MixinDebugScreenEntryList.java`
    - 20 — `GlStateManager`/`GlConst` usage differences — `VoxyRenderSystem.java`
    - 19 — fabric client command API (`ClientCommands` is 26.x; 1.21.1 uses `ClientCommandManager`) — `VoxyCommands.java`
    - 15 — NBT API newer than 1.21.1 (`CompoundTag.getIntOr/getStringOr/...`) — `Mapper.java`, `WorldImporter.java`
    - 13 — fog (26.x `FogRenderer`/`FogData`, sodium `FogParameters`) (task 5) — `Viewport.java`, `MixinFogRenderer.java`, `IrisUtil.java`
    - 7 — `LevelExtractor`/render-state extraction (26.x-only) (task 7) — `MixinClientLevel.java`, `MixinLevelRenderer.java`
    - 5 — `org.jspecify.annotations.Nullable` not on 1.21.1 classpath — `ICheekyClientChunkCache.java`, `MixinClientChunkCache.java`
- 2026-07-18 (afternoon): T3 done (both mechanical waves reviewed; compile3 = 370 errors, log in scratchpad). T9 mostly done (bakery/ReuseVertexConsumer ported + AW mip fix; LightMapHelper deferred to T4 wave). Design docs for T4/T5 (blaze3d-backport-design.md) and T6 (sodium-integration-design.md) completed by gpt-5.6-sol in write-mode re-runs (first runs lost output to read-only sandbox). T6 implementation in progress (gpt-5.6-terra). Blaze3d/fog implementation dispatches after sodium wave lands (avoids concurrent edits to client.voxy.mixins.json); fog holder reconciled to a single class (VoxyFogSnapshot).
- 2026-07-18 (late afternoon): Sodium wave landed (370→138 errors), blaze3d/fog wave landed (138→20), final import/registry fixes by sonnet (20→0). **compileJava GREEN.** Committed 87625f1b. Full `gradle build` running; next: runClient smoke test (WSLg DISPLAY available). Known accepted gaps: anyDarkendTex heuristic off; getLightEngine() returns null in colour-capture contexts (matches ref port); nvidium/vivecraft/flashback/chunky compat excluded (T13).
- 2026-07-18 (evening): Runtime phase. Iris bumped to 1.8.14-beta.1+1.21.1 (sodium 0.8.12 breaks iris <1.8.13) + loom-gate manifest patch extended to iris; LWJGL pin 3.4.1→3.3.3 (26.x leftover, sodium driver check); runtime mixin fixes: MixinWorld profiler param restored (had been wrongly dropped), MixinGPUSelect retargeted to Window ctor (ref-port approach), MixinBlockableEventLoop disabled (no 1.21.1 hook). **Mixin application fully clean; client boots to GL/asset init** (Mesa llvmpipe GL 4.5, DSA ok). Committed 9abe8eeb.
- 2026-07-18 (evening): T12 end-to-end test: dedicated-server world pregen + quickPlay join. World loads; sodium/iris/lithium render fine; **voxy crashed on join**: ctor-ordering NPE from the Java-21 super()-first rework (VoxyInstance ctor virtually calls shouldCreateInstance() → reads this.config before subclass assigns it; VoxyClientInstance.java:45). Fix + retest in progress.
- 2026-07-18 (final): Ctor-ordering fix committed (f61554af) and verified in-world. Mesa-override retest: shaders compile, voxy fully functional in-world (see T12 notes) — verdict YES under test env; remaining caveats are environment-only (llvmpipe GLSL cap, no libflite narrator). Final `gradle build` green.

## Final state / how to run

- Build: `./gradlew build` (Java 21). Jar: build/libs/voxy-0.2.18-beta.voxyserver.1.jar. Targets MC 1.21.1 fabric, requires sodium 0.8.12 (fabric.mod.json predicate auto-generated).
- Dev-run on WSL2/llvmpipe: prefix `MESA_GL_VERSION_OVERRIDE=4.6 MESA_GLSL_VERSION_OVERRIDE=460 ./gradlew runClient` (real GPUs with GL 4.6 need nothing).
- T13 DONE (2026-07-19): nvidium/vivecraft/flashback/chunky compat restored, gated at runtime instead of excluded at compile time. See "T13: mod-compat restoration" below. Known accepted gaps carried forward: anyDarkendTex heuristic disabled (no 1.21.1 API), getLightEngine() null in colour-capture contexts (matches ref port), MixinBlockableEventLoop disabled (no 1.21.1 hook point).

## T13: mod-compat restoration (2026-07-19)

Restored the four deferred optional-mod integrations (nvidium, flashback, chunky, vivecraft) behind runtime gating instead of leaving them excluded from compilation, following the reference 1.21.1 port's mixin-plugin approach.

- **build.gradle**: removed the `sourceSets` excludes for `client/mixin/flashback/**`, `client/mixin/nvidium/**`, `commonImpl/mixin/chunky/**` (the `vivecraft/**` exclude is left in place — there's no vivecraft mixin dir; its compat is plain runtime code, not a mixin). Added `modCompileOnly` deps for chunky/vivecraft/flashback via the existing `smartDependency()` helper, and nvidium via the same helper with an explicit `drouarb:nvidium` maven coordinate (its GitHub-releases ivy repo was already declared, just unused). All four are compile-only — no runtime/`modImplementation`, so Voxy never hard-depends on them. None tripped the Loom-version manifest gate (only sodium/iris needed `patchLoomVersionJar`). Also removed the `TEMP-REPRO-BLOCK` (quickPlay programArgs + jcpp/glsl-transformer runtimeOnly test hook).
- **gradle.properties**: `chunky_version=1.4.23-fabric`, `vivecraft_version=1.21.1-1.3.15-fabric`, `flashback_version=rNCr1Rbs` (0.34.0, matches the reference port's pinned version), `nvidium_version=0.4.1-beta9:1.21-1.21.1@jar` (matches reference port; passed straight through into the `drouarb:nvidium:<value>` ivy coordinate). All verified to exist for 1.21.1/fabric via the Modrinth API.
- **Runtime gating plugin**: `me.cortex.voxy.client.VoxyMixinPlugin` (registered as `"plugin"` in `client.voxy.mixins.json`) gates `mixin.flashback.*` on `isModLoaded("flashback")`, `mixin.nvidium.*` on `isModLoaded("nvidium")`, and `mixin.iris.*` on `isModLoaded("iris")` (iris is currently a forced `modImplementation` dep so this is defense-in-depth, not load-bearing). `me.cortex.voxy.commonImpl.VoxyMixinPlugin` (registered in `common.voxy.mixins.json`) gates `mixin.chunky.*` on `isModLoaded("chunky")`. Both model the reference port's `ClientVoxyMixinPlugin` design, adapted to per-subpackage gating (the reference plugin itself doesn't actually gate these mixins — see below). Re-added `flashback.MixinFlashbackMeta`, `flashback.MixinFlashbackRecorder`, `nvidium.MixinRenderPipeline` to `client.voxy.mixins.json` and `chunky.MixinFabricWorld` to `common.voxy.mixins.json`.
- **Per-mod mixin porting** (verified via `javap` against the loom-remapped jars in `.gradle/loom-cache/remapped_mods`):
  - `flashback.MixinFlashbackMeta` / `MixinFlashbackRecorder`: no changes needed — `FlashbackMeta.toJson()/fromJson()` and `Recorder(RegistryAccess)` match the reference port's mixin signatures exactly on flashback 0.34.0.
  - `chunky.MixinFabricWorld`: no changes needed — already present in this port pre-adapted to use `@Mixin(targets = "...")` + `Object instance` (avoiding a compile-time import of chunky's internal `ServerChunkCacheMixin`); `invokeGetChunkFutureMainThread` signature matches chunky 1.4.23 exactly.
  - `nvidium.MixinRenderPipeline`: **retargeted**. The version in the tree was an unported dev/26.x leftover using blaze3d types (`TerrainRenderPass`, `FogParameters`, `GpuSampler`, `GlTextureView`) that don't exist on 1.21.1's blaze3d. Rewrote against nvidium 0.4.1-beta9's actual `renderFrame(Viewport, ChunkRenderMatrices, double, double, double)` (confirmed via javap, matches the reference port exactly), and wired it into this port's real `VoxyRenderSystem` API (`setupViewport(Matrix4fc, Matrix4fc, VoxyFogSnapshot, int, int, double, double, double)` / `renderOpaque(Viewport, int, int)`) using the same `BoundFramebufferSnapshot.capture()` + `VoxyFogSnapshot.current()` pattern already used by `sodium.MixinDefaultChunkRenderer`, since nvidium — like sodium's `DefaultChunkRenderer` — renders into whatever framebuffer is currently bound rather than handing Voxy an explicit target.
  - `vivecraft`: no mixin dir (dev never had one — its compat is a direct API call in `ViewportSelector`, not a mixin). `VRRenderingAPI.instance().getCurrentRenderPass()` on vivecraft 1.21.1-1.3.15-fabric matches dev's code unchanged.
- **Un-stubbed compat classes**: `client/compat/FlashbackCompat.java` and the vivecraft branch of `client/core/rendering/ViewportSelector.java` restored to dev behavior (`git show b164a6d9:<path>`) verbatim — both already gated by `isModLoaded`/an installed-flag constant in the original dev code, so no porting was needed beyond removing the hardcoded-false stub.
- **Note on the reference port's actual plugin**: `ref_m3t4f1v3/mc_1211`'s `ClientVoxyMixinPlugin.shouldApplyMixin()` unconditionally returns `true` — it doesn't actually gate flashback/nvidium there. That's because the reference port declares nvidium as an always-present runtime dep (`modRuntimeOnlyMsk`) and doesn't handle chunky/flashback/vivecraft absence at all (compile-only, no gating, so a runtime environment without them would crash on those mixins). This port intentionally diverges and adds real `isModLoaded` gating for all four, since the task requires them to work as truly optional runtime compat, not implicit requirements.
- **Verification**: `./gradlew compileJava` green on the first attempt after all changes (no iterative fixing needed — the javap-verified signatures were correct up front). Boot smoke (`MESA_GL_VERSION_OVERRIDE=4.6 MESA_GLSL_VERSION_OVERRIDE=460 ./gradlew runClient`, flashback/nvidium/chunky/vivecraft absent from the mod list) reached a clean idle main-menu state: Fabric Mods list confirms none of the four are loaded, mixin application logged zero errors (`Loaded Fabric development mappings`, `MixinExtras` init, then straight into Voxy's own config-registration/init log lines with no `InvalidMixinException`/`ApplicatorException`), Iris still builds its pipeline normally. One unrelated crash was hit and fixed along the way: leftover `run/config/iris.properties` state from earlier unrelated shader-repro testing had `enableShaders=true` pointing at a shaderpack requiring `org.anarres.cpp` (jcpp), which the just-removed `TEMP-REPRO-BLOCK` had supplied as a `runtimeOnly` dep; flipped `enableShaders=false` in the local run config (not a code change) to get a clean boot. Also hit the pre-existing, already-documented `libflite`/narrator `UnsatisfiedLinkError` (WSL2 has no flite native lib) — non-fatal, caught by Minecraft's own narrator init, matches the "no libflite narrator" gap already recorded above.
- **Nothing left outstanding for T13**: all four mods ported/gated, nothing skipped.

## Final review pass (2026-07-18)

Multi-angle review (5 finder agents + verification; 8 findings reported via harness):
- FIXED (confirmed correctness): (1) RenderStateSnapshot.restore() now syncs GlStateManager's per-unit texture cache (was raw glBindTextureUnit desyncing MC's bind cache → wrong textures on later vanilla draws); (2) IrisUtil.clearIrisSamplers() call restored in renderOpaque's finally (lost in blaze3d rewrite); (3) black tint 0x000000 no longer conflated with no-tint in ModelFactory (only -1 = absent); (4) dead MixinBlockableEventLoop.java deleted.
- REFUTED by verification: fog-cutoff regression (voxy uses its own captured fog, vanilla fog only affects vanilla terrain), reverse-Z detection timing (nothing on the 1.21.1 stack enables reverse-Z), iris fog-snapshot staleness (iris 1.8 keeps vanilla setupFog in the path), tint-probe exception swallowing (ref-port parity).
- Future-work findings (documented, not blocking): ~90 glGet round-trips/frame in the GL state snapshot (could track only mutated state), latent unsafe no-arg VoxyInstance ctor pattern, local sRGB encode diverging from sodium's, duplicated single-state stub BlockGetters, MixinGPUSelect name no longer matching its Window target.
- Post-fix verification: compileJava green; 5-min boot smoke clean (no mixin/injection errors, voxy init normal).
- 2026-07-18 (post-release fix): User ran the jar in a real 1.21.1 NeoForge modpack (via Sinytra Connector) — hard crash: session/MixinMinecraft still targeted 26.x `disconnect(Screen;ZZ)V` (1.21.1 has `(Screen;Z)V` as the terminal overload). Loom's dev runtime tolerates dead injection targets, so runClient smoke tests never caught it. Fixed + full audit of EVERY mixin descriptor (@Inject/@Shadow/@Accessor/@At INVOKE/FIELD) against the mapped 1.21.1 jar, iris 1.8.14-beta.1 jar, and sodium 0.8.12 sources: the disconnect target was the only mismatch. Rebuilt + boot-smoke green. Lesson recorded: production mixin validation is stricter than dev; audit descriptors statically, don't trust dev runClient alone.
